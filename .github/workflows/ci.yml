name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  quality-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      # curfew / consistency / certification-language / language audits,
      # then eslint, tsc, and the production build
      - run: npm run check
        env:
          VITE_SUPABASE_URL: https://ykykeioydvtxpyreshhs.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_9NOklH5Dvj3PcQs2dCLdpg_zx4txe8a
